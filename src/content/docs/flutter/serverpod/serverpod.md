---
title: бэкенд для flutter
---

Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)

## Заголовок 2

- Создать генератор юз-кейсов для связанных таблиц (RelationUseCaseGenerator)
- Модифицировать фабрику генераторов, чтобы включить новые генераторы
- Обновить класс GenerateAllFilesCommand, чтобы правильно выбирать генераторы в зависимости от типа таблицы
- - Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)

## Заголовок 1

- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)

Давайте обсудим план действий и согласуем структуру файлов для этого функционала.

### Заголовок 3

здесь описание 
- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)
- Исходя из структуры кода, следующим логичным шагом будет **создание генераторов для отношений между таблицами**. Вам нужно создать код для:


- Создать генератор репозиториев для связанных таблиц (RelationRepositoryGenerator)
- Создать генератор локальных источников данных для связанных таблиц (RelationLocalDataSourceGenerator)


